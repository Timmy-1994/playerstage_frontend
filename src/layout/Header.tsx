import React, { useEffect } from 'react';
import styled from 'styled-components';
import { useHistory } from 'react-router-dom';

export default function AIEDRHeader() {

    const history = useHistory();

    return (
        <div>header</div>
    );
};
